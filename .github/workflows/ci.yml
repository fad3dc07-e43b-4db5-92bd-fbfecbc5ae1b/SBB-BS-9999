name: CI
# harmless comment to trigger workflow

on:
  pull_request:
    branches:
      - main
    paths:
      - "artifact/**"
      - ".github/workflows/**"

  push:
    branches:
      - main
    paths:
      - "artifact/**"
      - ".github/workflows/**"

jobs:
  execute_rules:
    name: Validar cumplimiento
    uses: ContinuousArchitecture/CALinter/.github/workflows/compliance.yml@main
